<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/ActionBarTheme</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:actionOverflowButtonStyle">@style/OverFlow</item>

    </style>

    <style name="ActionBarTheme" parent="android:Widget.ActionBar">
        <item name="android:background">@color/actionBarBackground</item>
        <item name="android:titleTextStyle">@style/TitleTextStyle</item>
        <!--<item name="android:displayOptions">showHome|showTitle</item>-->
        <!--<item name="android:icon">@drawable/ic_launcher</item>-->
    </style>

    <style name="TitleTextStyle" parent="ActionBarTheme">
        <!--<item name="android:textColor">@color/actionBarTitleText</item>-->
        <!--<item name="android:textSize">@dimen/actionBarTitle</item>-->
    </style>

    <!-- Styles -->
    <style name="OverFlow" parent="@android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_overflow</item>
    </style>

</resources>
